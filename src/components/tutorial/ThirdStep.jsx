export default function ThirdStep() {
  return (
    <p>
      Here a form where you can fill all the information about your request.
      <br />
      You can see that inputs are already filled : as an example for the
      tutorial, you want to get from your API all the red shoes with a 10
      results limit.
    </p>
  );
}
